after generating token, linux, source .env in base directory.

then run the curl commands using the $TOKEN variable.
which will at that point be in the source

first, go to your .env file and define the $TOKEN variable

try
echo $TOKEN

if you don't see the token,

then, in termial
source .env

below is the curl command




# 1. Login / get jwt auth code. POST request, no need for the $TOKEN var

curl -X POST http://localhost:8080/auth/login -H "Content-Type: application/json" -d '{"username": "user1", "user_id": 1, "role": "rider"}'


# 1. Get Drivers List (GET)

curl -X GET http://localhost:8080/drivers -H "Authorization: Bearer $TOKEN"


# 2. Request Ride (POST)

curl -X POST http://localhost:8080/request-ride -H "Content-Type: application/json" -H "Authorization: Bearer $TOKEN" -d '{"lat": 0.3135, "lng": 32.5811}'


# 3. Check Ride Status (GET)

curl -X GET http://localhost:8080/ride-status/123 -H "Authorization: Bearer $TOKEN"


# 4. Call logout:

curl -X POST http://localhost:8080/auth/logout -H "Authorization: Bearer $TOKEN"



curl -X POST http://localhost:8080/request-ride -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjoxMjMsInVzZXJuYW1lIjoidGVzdHVzZXIiLCJyb2xlIjoicmlkZXIiLCJ2ZXJzaW9uIjoxLCJpc3MiOiJyaWRlLXNoYXJpbmctYmFja2VuZCIsImV4cCI6MTc0MzM3MTY3Nn0.tA7dG_MWkBt7PkZ-4S9umF4e1MH41tRKm_RKZBJ9hkw" -H "Content-Type: application/json" -d '{"lat":0.3135, "lng":32.5811}'




tried to run with docker and encountered an issue?

# Stop any running containers
docker-compose down

# Remove unused volumes
docker volume prune -f

# Rebuild with clean cache
docker-compose build --no-cache

# Start the services
docker-compose up

